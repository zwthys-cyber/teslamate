# 第一轮升级：连接与数据可靠性

基于提交 `3e7612acfba029bf388c9e592a5fafbf2b6e5daf`。

## 已完成

- 将构建工作流迁至仓库根目录，配置 `ios` 工作目录，先运行 XCTest 再打包。
- 验证新服务器后才保存连接；验证或钥匙串写入失败时保留原配置。
- 通过连接版本隔离响应，防止旧请求覆盖新服务器数据；取消和断开连接也会使请求失效。
- 钥匙串改为更新优先并检查 OSStatus，增加断开连接和清除凭据入口。
- 增加地址校验，移除个人服务器默认地址，支持服务器子路径。
- 刷新失败时保留记录并提示错误，展示本机获取时间；回到前台时刷新。
- 未知电量与锁状态保持未知，状态图标区分行驶、充电、休眠和离线；外观跟随系统。
- 移动 API 校验车辆 ID 和记录 ID 的数据库范围，无车辆返回结构化 404，鉴权响应禁止缓存。

## 验证记录

- 12 个 XCTest 通过：连接切换、失败和取消、凭据保存失败、清除连接、URL 校验、请求构造、解码和网络错误。
- 本机使用 Swift 5.10.1 Linux 工具链运行同一份核心源码和测试；临时测试适配器替代 Apple Keychain，并补充 Linux SDK 缺少的 async URLSession 方法。
- 8 个移动 API 测试通过：使用 Elixir 1.20.2 / OTP 29 和 PostgreSQL 18 独立临时容器，未连接生产数据库。
- Swift 源码语法、YAML、plist、工作流 bash 语法及 `git diff --check` 通过。
- 尚未运行 Xcode 模拟器、完整 iOS 编译、Apple Keychain 集成或真机测试，也未生成本轮 IPA。

## 下一阶段

1. 在 macOS 验证构建及 iOS 17+ 真机连接、钥匙串、前后台切换和错误提示。
2. 增加多车选择、行程与充电页面，配套 API 游标分页和日期筛选。
3. 定义车辆采样时间字段、离线持久化、单位与统计口径。
4. 收紧 HTTP 例外并完成公网接入边界；当前仍兼容原有私网 HTTP 部署。
5. 完成大字体、VoiceOver、地图跟随行为及 TestFlight 发布准备。

首页的本机获取时间不代表车辆采样时间；当前没有新增后台轮询或车辆唤醒请求。

# 第二轮升级：多车、行程和充电

## 已完成

- 新增车辆、行程、充电、设置四个原生标签页。
- 多车选择在本机保存；车辆列表刷新保持选择，车辆消失时回退；换服务器清除旧选择。
- 行程与充电列表支持加载更多、日期筛选、下拉刷新、空状态及失败重试。
- 切车或换服务器重建历史导航；改日期和刷新使旧请求失效，避免串车和旧分页覆盖。
- 行程详情展示路线、起终点、里程、时长和速度；充电详情展示电量、功率曲线及费用。
- 明确区分免费和费用未记录；日期筛选按本地日历处理夏令时。
- 后端使用时间与 ID 游标分页，详情校验可选车辆范围，新增查询索引。
- 轨迹及曲线最多返回 2000 个采样点，保留首尾并提供抽样说明。
- 旧后端缺少必要字段时给出升级提示，详情见 [接口约定](docs/MOBILE_API.md)。

## 验证记录

- 27 个 Swift 核心 XCTest 通过（含第一轮）：增加多车选择、翻页去重、失败重试、
  日期切换竞争、刷新与加载更多竞争、错车响应、游标循环、夏令时及详情解码测试。
- 14 个移动 API 测试通过（含第一轮）：覆盖相同时间戳与新增记录时的分页、
  时间边界、非法筛选、跨车辆详情、缺失记录、采样上限和零值保留。
- 本轮新增索引迁移已在临时 PostgreSQL 中执行；没有在生产库执行。
- Swift 核心测试仍使用第一轮的 Linux 适配环境；新增 SwiftUI/MapKit/Charts 页面尚待 Xcode 完整编译和真机验证。

## 服务器只读联调

已确认实际服务器运行 `local/teslamate:3e7612ac`，通过 Tailscale 访问。
车辆、行程、充电及详情返回 200，未鉴权车辆请求返回 401。
现有服务器没有分页元数据、历史记录的车辆 ID 和抽样元数据，需要配套部署本轮后端。
检查时实时电量、车锁、位置等字段为空，历史路线与充电采样可读；尚未诊断采集端原因。
没有重启服务、部署代码或更改生产数据库。服务器凭据未纳入项目。

## 后续交付边界

先验证完整 iOS 构建与真机交互，再安排后端部署和新版 App 联调。
离线持久化、统计页面、采样时间、单位设置、HTTP 例外收紧和 TestFlight 仍是后续工作。
